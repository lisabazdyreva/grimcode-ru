# Техническое имя проекта: строчные латинские буквы, цифры и подчёркивания, не длиннее 49 символов
# Длиннее — PostgreSQL молча обрежет имена баз до 63 байт: к слагу добавляется _notifications
PROJECT_SLUG=template

# Диапазон портов для локального запуска
PORT_RANGE_START=63000
PORT_RANGE_END=63020

# Локально - первый localhost порт из диапазона портов для локального запуска
# В продакшне - внешний адрес проекта без порта
PUBLIC_SITE_URL=http://127.0.0.1:63000

# Порт, на котором слушает приложение. Основной копии — первый порт диапазона, каждому worktree
# свой; выбирает bootstrap. В продакшне платформа может задать PORT, он имеет приоритет.
GATEWAY_PORT=63000

# Строка подключения к PostgreSQL. Локально — сервер на самой машине; в продакшне — управляемая база.
# Этой учётной записью модуль создаёт свою базу при первом запросе, поэтому у неё должно быть
# право CREATE DATABASE. Каждый модуль работает в базе <PROJECT_SLUG>_<модуль>.
#
# Локальный сервер один на машину: все копии проекта и все worktree живут в нём, а разделяет их слаг
# в именах баз. Учётная запись создаётся один раз:
#   sudo -u postgres createuser --createdb --pwprompt template
DATABASE_URL=postgres://template:template@127.0.0.1:5432/postgres

# Сколько времени живёт сессия, в секундах
AUTH_SESSION_TTL_SECONDS=2592000

# Как отправляем письма: log (письма сохраняются локально) / unisender (Unisender Go)
EMAIL_PROVIDER=log
EMAIL_FROM_ADDRESS=no-reply@example.com
EMAIL_FROM_NAME=Template

# Ключ Unisender Go, если используется
UNISENDER_GO_API_KEY=
